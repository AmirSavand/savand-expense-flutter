# shared

Reusable code used across two or more features.

## Rules

- No feature-specific logic, if it belongs to one feature, it stays in that
  feature.
- No business logic, only UI components, utilities, and helpers.
- Anything added here should be generic enough to work in any context.
